import { Metadata } from 'next';
import React from 'react'

export const metadata: Metadata = {
  title: 'Customers',
};

const page = () => {
  return (
    <div>Customer Page</div>
  )
}

export default page